
pipeline {
    agent {
        docker {
            image maven:3-alpine
            args \cf4 \cb5 '\cf6 -v /root/.m2:/root/.m2
        }
    }
    stages {
        stage(Build ){ 
            steps {
                mvn -B -DskipTests clean package
            }
        }
    }
}}
